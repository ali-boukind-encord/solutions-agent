CONTEXT
Fyxer CTO & Head of Human Annotations. They're scaling from 0-$3M ARR, drowning in manual Google Sheets annotation (20 fine-tuning use cases). One misclassified email costs them customers. They need speed, accuracy, structure—fast.

DEMO FLOW
1. Load the Fyxer demo project. Show the two sample emails (deal opportunity HTML + marketing promo) to establish relevance immediately.
2. Render one email in HTML view, then toggle to text/PDF—demonstrate Encord handles all email formats they receive, no copy-paste needed.
3. Walk the Email Category classification: show how selecting "Important" reveals nested attributes (Type, Urgency, Requires Reply). Emphasize this replaces their chaotic spreadsheet logic.
4. Highlight the Key Entity text object—show tagging a person ($48K deal mention, dates, monetary values). Explain this feeds fine-tuning datasets automatically.
5. Complete annotation on deal email. Show "Annotator Notes" (free text) for edge cases. Stress: one QA pass, no validation spreadsheet.
6. Compare: "In Sheets, this took 8 minutes and lives in 5 places. Here, it's structured, exportable as JSON, and ready for model training."
7. Close on workflow impact: 15+ annotators → Encord scales. No cognitive load. Consistent taxonomy.

THINGS TO KEEP IN MIND
- Albert knows the annotation space well; acknowledge competitive features (ease, speed, structured extraction) without defensive language.
- Nesting is your superpower here—show how it mirrors decision trees they already use mentally, just captured cleanly.
- Emphasize JSON export and fine-tuning readiness. They care about downstream model performance, not UI prettiness.
- Don't overcomplicate. Fyxer is shipping fast; show speed of task setup and annotation, not advanced features.
- One email should be completed in under 3 minutes to prove efficiency vs. Sheets.

INDUSTRY/TECHNICAL CONTEXT
- Email classification for CSM/revenue teams is high-stakes: revenue leakage from misclassification is quantified ($48K example in email).
- Fine-tuning workflows benefit from nested, multi-label structure; flat taxonomies lose signal. Encord's ontology mirrors their mental models.
- They're building tone-aware reply drafting, so capturing tone classification and quality signals feeds that training loop.
- Structured data extraction (entities, dates, amounts) is their stated gap vs. competitors; text objects solve this without forcing bounding boxes.
- JSON/structured export is non-negotiable for downstream fine-tuning pipelines; emphasize deterministic output and no manual translation.