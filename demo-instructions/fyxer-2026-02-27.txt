FYXER EMAIL SORTING & FINE-TUNING DEMO — SE CHEAT SHEET

CONTEXT
Albert (ex-Invisible, Head of Human Annotations) + CTO evaluating platforms for email classification at scale. They're growing 0→$3M ARR with 20 fine-tuning use cases. Currently stuck in Google Sheets. Care about: accuracy, speed, structured data extraction, cost per annotation, and ease of iteration.

DEMO FLOW
1. Open project. Show two real emails (deal opportunity + marketing newsletter). Frame: "These look similar on surface but need different downstream handling."
2. Classify first email as "Important > Deal/Revenue" + Urgency level. Show nested attributes collapsing/expanding. Emphasize: one click handles complexity instead of multiple spreadsheet columns.
3. Highlight text-span annotation: select "$47 portfolio companies" as Key Entity (Monetary Value), select "30 minutes this week" as Action Item (Action Owner: Sender). Show this extracts structured data for fine-tuning.
4. Add "Requires Draft Response > Yes" with nested Tone + Priority. Show how annotation directly feeds their email drafting model.
5. Capture "Annotator Notes" (text field). Explain: audit trail + training signal for QA.
6. Flip to second email, classify as "Marketing > Newsletter" + Relevance. Show speed: 60 seconds vs manual sheets.
7. Close with: "20 use cases, one unified ontology. Scales to 50+ annotators. Integrates with your fine-tuning pipeline via API."

THINGS TO KEEP IN MIND
- Albert knows the annotation space deeply. Don't oversell. Focus on measurable gains: speed per annotation, error reduction (misclassifying deal as spam = lost revenue), QA automation.
- Emphasize nesting as differentiator: Google Sheets requires separate columns or sheets; we flatten complex logic into one clean interface.
- Don't get lost in features. Stay on: structured data extraction (emails→JSON), iteration speed, and how this reduces their 20 use-case sprawl.
- Mention API/export for their fine-tuning pipeline. They care about seamless handoff to model training.
- Avoid: heavy product walkthrough. This is a working example, not a tutorial.

INDUSTRY/TECHNICAL CONTEXT
- Email classification for CSM/revenue ops is high-stakes: wrong categorization = lost deals, unhappy customers, churn.
- Fine-tuning LLMs on domain-specific email tone requires clean, labeled data. Annotation errors compound at scale.
- Structured extraction (entities: names, amounts, deadlines; actions: owner, due date) is non-negotiable for email automation use cases.
- Their prior embeddings initiative failed; they're now pivoting to traditional supervised fine-tuning. Show Encord supports that workflow cleanly.
- Google Sheets at 50+ annotators = collaboration chaos, no versioning, no QA workflow. Position Encord as purpose-built for this.